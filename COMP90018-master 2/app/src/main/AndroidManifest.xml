<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comp90018">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddScheduleActivity" />
        <activity android:name=".SetAlarmTimeActivity" />
        <activity android:name=".SetAlarmToneActivity" />
        <activity android:name=".SetColorActivity" />
        <activity android:name=".SetLocalActivity" />
        <activity android:name=".SetRePlayActivity" />
        <activity android:name=".ScheduleDetailActivity" />
        <activity
            android:name=".MainPageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>